doctype html
html(lang="en")
  head
    meta(charset="UTF-8")
    title= title || key
  body
    div(id="app")

    if initialState
      script(type="text/javascript").
        initialState = !{JSON.stringify(initialState)}

    script(src="/bundle/#{key}.js")
